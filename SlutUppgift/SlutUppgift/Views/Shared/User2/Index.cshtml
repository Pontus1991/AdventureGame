@model IEnumerable<SlutUppgift.Models.User2>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Login/Pages/Shared/_LoginPartial.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.UserName2)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PassWord2)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.UserName2)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PassWord2)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
